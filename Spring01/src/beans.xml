<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">

	<!-- 
	<alias name="" alias="" />:设置别名

	<bean id="id唯一标识符" name="别名" class="class是bean的全限定名包名+类名"
		scope="bean的作用域，默认为单例模式
			singleton 单例模式 整个容器只有一个对象
			Prototype 原型模式 每次获取Bean都创建一个新的对象
			Request  每次请求创建一个新的对象
			Session 在会话范围内产生一个新的对象"
		init-method="对象初始化方法" destroy-method="对象销毁方法"
		autowire="自动装配 简化spring配置
	byName 根据set方法名去查找相应的bean
		byType 根据类型查找
		Constructor 通过构造器创建实例时">
	</bean>:对象

	<import resource="" />:引用其他配置文件
	-->
</beans>
